<h1>Listing teachers</h1>

<table>
  <thead>
    <tr>
      <th>Jméno</th>
      <th>Popis</th>
      <th>Skupina</th>
      <th>Zkratka</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teachers.each do |teacher| %>
      <tr>
        <td><%= teacher.name %></td>
        <td><%= teacher.description %></td>
        <td><%= teacher.group %></td>
        <td><%= teacher.abbr %></td>
        <td><%= link_to 'Zobrazit', teacher %></td>
        <td><%= link_to 'Upravit', edit_teacher_path(teacher) %></td>
        <td><%= link_to 'Odstranit', teacher, method: :delete, data: { confirm: 'Jste si jisti?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Teacher', new_teacher_path %>
