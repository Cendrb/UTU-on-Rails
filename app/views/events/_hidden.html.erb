<div class="event" data-id=<%= event.id %>>
	<%= button_to 'Obnovit', event_reveal_path(event), method: :post, remote: true, class: 'hide_or_reveal' %>
	
	<h3 class="details_done"><%= event.title %></h3>
</div>
