<%= form_for(@sgroup) do |f| %>
    <% if @sgroup.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@sgroup.errors.count, "error") %> prohibited this sgroup from being saved:</h2>

          <ul>
            <% @sgroup.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :timetable_abbr %><br>
      <%= f.text_field :timetable_abbr %>
    </div>
    <div class="field">
      <%= f.label :group_category_id %><br>
      <%= f.collection_select(:group_category_id, GroupCategory.all, :id, :name) %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
