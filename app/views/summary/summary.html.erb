<h1>Přehled</h1>
<% if notice %>
<p id="notice">
    <%= notice %>
</p>
<% end %>
<% if alert %>
<p id="alert">
    <%= alert %>
</p>
<% end %>
<h2>Úvod</h2>
<p>Vítejte na webové verzi Utu. Veškerá data jsou uložena pouze ve zdejší databázi a i desktopová aplikace tento web využívá. Tato stránka "běží na" frameworku <%= link_to 'Ruby on Rails', 'http://rubyonrails.org/' %>, který mimo jiné využívá třeba Twitter.

<% if logged_in? %>
    <h2>Služba</h2>
    <% if @error %>
        <%= @error %>
    <% else %>
    <% if @service %>
        <p>
            Vaše období služby začíná <%= l @service.service_start %> a končí <%= l @service.service_end %>
        </p>
        <%= link_to 'Více informací o vaší službě', @service %>
        <% else %>
        <p>Nebyly nalezeny žádné služby</p>
        <% end %>
    <% end %>
    
    <% else %>
    <h5>Pro zpřístupnění následujících funkcí se prosím <%= link_to 'přihlašte', login_path %> nebo <%= link_to 'zaregistrujte', register_path %>.</h5>
    <ul>
        <li>
            personalizované zobrazení služby
        </li>
        <li>
            filtrované zobrazení položek
        </li>
        <li>
            možnost skrytí libovolné položky
        </li>
        <li>
            a postupem času i mnoho dalšího ;)
        </li>
    </ul>
<% end %>
<% if @near_payments && @near_payments.count > 0 %>
    <h2>Platby</h2>
    <% @near_payments.each do |payment| %>
        <h4><%= payment.title %></h4>
        <p>Zaplatit <%= days_to_pay_cs (payment.pay_date - Date.today).to_i %> <%= number_to_currency(payment.price, locale: :cs) %>  (<%= l payment.pay_date %>)</p>
    <% end %>
<% end %>

<h2>Manuální zobrazení podrobností z libovolného období</h2>
<%= form_tag(details_path, method: 'get') do %>
    <div class="field">
        <%= label_tag :from, 'Od:' %>
        <%= date_select :range, :from, order: [:day, :month, :year] %>
    </div>
    <div class="field">
        <%= label_tag :to, 'Do:' %>
        <%= date_select :range, :to, order: [:day, :month, :year] %>
    </div>
    <div class="field">
        <%= label_tag :group, 'Skupina:' %>
        <%= number_field_tag :group, '1' %>
    </div>
    <div class="field">
        <%= submit_tag 'Zobrazit' %>
    </div>
<% end %>

