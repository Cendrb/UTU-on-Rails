<%= form_for(@day_bitch) do |f| %>
  <% if @day_bitch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@day_bitch.errors.count, "error") %> prohibited this day_bitch from being saved:</h2>

      <ul>
      <% @day_bitch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :bitch_id %><br>
    <%= f.collection_select :bitch_id, @bitches, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
