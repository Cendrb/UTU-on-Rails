<% type = item.get_utu_type %>

<%= link_to mi.visibility.md_36, reveal_item_path(id: item.id, type: item.get_utu_type), remote: true, class: 'shared_show_hide_or_reveal' if logged_in? %>

<h3 class="details_done"><%= item.title %></h3>
